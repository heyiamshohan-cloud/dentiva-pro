# Dentiva Pro v1.4.0 — Execution State (persistent checkpoint)

> This file is the authoritative continuation checkpoint. After any interruption, read this
> file, inspect the repository, and continue from "Exact next action" without repeating
> completed work.

**Last updated:** 2026-09-22 · Session branch: `arena/01a0c8bf-dentiva-pro`

## Current phase

**PHASES 0–11 COMPLETE. PHASE 12 (release) IN PROGRESS — first CI validation run found
three first-run/auth defects; all fixed + regression-tested; second validation run pending.**

CI run **`35734256848`** (validation gate, no publish token) passed tests/build/Chromium
install but failed the rewritten visual spec. CI logs are unreadable from this sandbox
(blob EOF), so a jsdom harness ran the real production bundle locally and reproduced the
failure: the first-run setup modal could not advance. Root causes (all fixed, all covered
by the new `tests/first-run.test.mjs`, 5 tests on both runtimes):

1. `src/ops.js` `userUpsert`: admin-coverage guard counted only stored users → the very
   first `user.create` (setup Administrator) was rejected. Now projects the post-upsert
   population (create adds the new record; edit replaces the stored row).
2. `src/api.js` `#applyPinToSet` + legacy upgrade: spread `{salt, hash}` instead of the
   repo's `pinHash`/`pinSalt` keys → browser first-run account had no usable PIN (setup
   looped on every open; sign-in said "no PIN set").
3. `electron/lib/auth.mjs` `verifyPin`: compared `Buffer.from(toHex(derived))` (64 UTF-8
   bytes of the hex string) against the 32-byte digest → **every Electron sign-in failed**.

Post-fix state: `npm test` **60/60**, Vite build green (338.89 kB / 38.10 kB), and the
jsdom harness (fresh profile → boot → 3-step setup → PIN sign-in → 16 surfaces) passes
end-to-end against BOTH the production bundle and the vite-dev module graph. The visual
spec also had its own dashboard-h1 assertion fixed (dashboard header is a greeting, not the
nav label).

**Validation run history (no publish token on any of these):**
- Run `35734256848` (25111ab's predecessor): visual spec failed → 3 defects fixed (above).
- Run `35737821942` (25111ab): visual spec STILL failed (12/12 tests, ~48s — same
  signature: setup modal not reachable in Chromium). jsdom passes, so the cause is
  Chromium-specific (CSP enforcement / real network / browser API differences) — not
  yet identified.
- Run `35738397942` (56cf801): added the annotation diagnostics channel; proved
  `::error` annotations ARE readable via the checks API from this sandbox; error text
  came from the wrong JSON path and page-state captures were missing.
- Run `35740200177` (35d624d, current tip): diagnostics hook now emits annotations from
  the test hook itself (stdout `::error`, sanitized), per-probe snapshot try/catch,
  fixed JSON error extraction (`tests[].results[].error.message`).

**Sandbox GitHub token expired mid-session** (GH_TOKEN 401 'Bad credentials'; `git ls-remote`
also auth-fails). Run 35740200177 continues on GitHub regardless. After reconnecting
GitHub in Arena: read its annotations (`gh api repos/heyiamshohan-cloud/dentiva-pro/actions/runs/35740200177/jobs`
→ job id → `/annotations`) — the `visual-fail` + `page-state` lines name the exact failing
expectation and dump pageErrors/console/requests/DOM/store state. Fix the root cause
(locally reproducible via the jsdom harnesses in /tmp/domtest.mjs + /tmp/devgraph.mjs),
then continue the release sequence below.

## Phase map

| # | Phase | Status |
|---|-------|--------|
| 0 | Recover v1.3.0 baseline (repo was empty on `main`; merged tip of `arena/01a0c66a-dentiva-pro` @ `93d6416`, which is v1.3.0 + release-audit doc) | DONE |
| 1 | Forensic audit + governance docs (`V1.4.0_BASELINE_AUDIT.md`, `V1.4.0_REQUIREMENTS_MATRIX.md`, this file) | DONE |
| 2 | Storage engine: relational schema on built-in `node:sqlite`, WAL, FKs, indexes, migration from v4 sql.js blob DB + legacy JSON + corrupt/future `.bak` recovery, no artificial caps | DONE (verified: `/tmp/migtest.mjs`) |
| 3 | Service layer: `src/ops.js` (~50 ops) + `src/queries.js` registries, server-side validation/RBAC/audit, PBKDF2 v2 auth + lockout, backup/restore engine (SHA-256 manifest, safety backup, module-group JSON restore), diagnostics | DONE (verified: `/tmp/opstest.mjs`, `/tmp/bktest.mjs`) |
| 4 | IPC + preload: 19-channel allowlisted `invoke`, session mgmt in main, ESM main (`electron/main.mjs`) importing shared service layer, hardened window, CSP, print pipeline, DENTIVA_SMOKE phases | DONE (commit `b7e3e65`) |
| 5 | Renderer rework: async `src/api.js` client (DesktopApi/LocalApi), paginated lists, server aggregates, browser LocalRepo parity | DONE (verified: `/tmp/localtest.mjs` identical suite vs LocalRepo) |
| 6 | Design System 2.0: `src/styles.css` (light-only, tokens, 581 lines, `@media 760px`), ⌘K palette, dashboard 3.0 | DONE |
| 7 | Feature expansion: all v1.3.0 workflows preserved + queue 2.0, dental chart 2.0, timeline, statements, accounting 2.0 w/ aging, analytics, module-grouped restore UI, diagnostics, notifications 2.0, saved views, first-run setup | DONE (commit `1d714fb`) |
| 8 | Localization audit (EN/BN 449 entries 1:1), printing/PDF 2.0 (isolated window, blocked-content guard) | DONE |
| 9 | Testing: `npm test` **60/60** (incl. new `first-run.test.mjs`); ops suite ~60 checks (SQL + browser parity); backup round-trip; benchmarks 1k/10k/25k/100k on real store; visual spec rewritten for v1.4.0 DOM — 3-step first-run setup + PIN sign-in driven, 16 surfaces × 6 viewports, overflow + accessible-name checks; jsdom production-bundle harness | DONE (visual spec + first-run fixes pending validation CI run #2) |
| 10 | Hardening: `npm audit` 0 vulns, zero runtime deps, placeholder scan clean, Electron hardening (context isolation, sandbox, nav guards, CSP, single-instance), license file present | DONE |
| 11 | Docs: README 1.4.0, USER_GUIDE, BUILD, PERFORMANCE_BASELINE, root CHANGELOG + `docs/CHANGELOG.md`, `FINAL_AUDIT_REPORT_1.4.0.md` (§6 pending CI), CI trigger fix | DONE (commit `0ece474`) |
| 12 | Versioning 1.4.0 everywhere; CI branch update; validation run green; controlled `[publish-release]` commit; publish CI run; verify artifacts; tag `v1.4.0` (created by workflow `gh release create`); GitHub release with 4 artifacts; fill audit report §6 | IN PROGRESS — run `35734256848` (first attempt) failed the visual spec → 3 defects fixed + regression-tested → validation run #2 pending |

## Completed requirements (headline)

- Baseline recovered and verified: 46/46 pass on v1.3.0 code at merge time; v1.4.0 suite now **55/55 pass**.
- Environment: Node v22.22.3, npm 10.9.8; `node:sqlite` (SQLite 3.51.3) works without flags; Electron 44.4.3 + electron-builder 26.15.3; `better-sqlite3` rejected (sandbox TLS blocks prebuilt binaries; zero packaging risk with built-in module).
- No artificial limits anywhere: no DB size cap, no attachment cap, no record caps (security test asserts).
- Scale benchmark (real store, `scripts/dataset-benchmark.mjs`): 100,000 patients / 945,086 records — page-1 list 18 ms, Bengali search 221 ms, revenue report 138 ms, integrity 2.4 s, backup of 412 MB store 1.6 s.
- Money invariants: integer cents everywhere; overpayment blocked; reprice locked post-money (`invoice-locked`); refunds never mutate originals; adjustments capped at due; patient balance consistent across SQL, migration and browser repos (including forgiveness adjustments).
- Security: PBKDF2-SHA-256 210k (legacy 120k verified + upgraded), 5/30 lockout persisted, server-side RBAC on every op/query, audit server-side, PDF print content validated, no telemetry, single allowlisted IPC surface.

## Active work

- Push the fix commits (no token) → validation CI run #2.
- On green: controlled release commit with `[publish-release]` in the message → push →
  publish run → verify all gates → record artifact digests in `FINAL_AUDIT_REPORT_1.4.0.md`
  §6 and this file → final docs commit (no token).

## Remaining work (summary)

Phase 12 only:
1. Validation run `35734256848` must pass (all steps; visual spec is the new unknown —
   if it fails, fix the spec/CSS, commit, push a new validation run, repeat).
2. Controlled release: commit message containing `[publish-release]` (workflow publish gate:
   `contains(github.event.head_commit.message, '[publish-release]')`).
3. Publish run gates: `npm run check` → `npx playwright install chromium` → `npm run test:visual`
   (6 viewports) → `npm run dist:win` (portable + NSIS) → PE `MZ` checks → `scripts/windows-smoke.ps1
   -PortableOnly` (create + restart persistence via DENTIVA_SMOKE) → ZIP assembly + inspection +
   SHA-256 checksum file → `gh release create v1.4.0` (creates the tag at the run SHA, 4 assets,
   refuses if the release already exists).
4. After publish: `gh release view v1.4.0 --json assets` → record sizes + SHA-256 digests in
   `docs/FINAL_AUDIT_REPORT_1.4.0.md` §6 + §5, update this file, commit docs (message WITHOUT
   the token), then declare the release complete.

## Known blockers / environment constraints

1. GitHub blob downloads (objects.githubusercontent.com) fail TLS in this sandbox → Windows
   artifacts are produced/verified only by the GitHub Actions Windows workflow (same as v1.3.0).
2. Electron binary cannot be downloaded locally → no local Electron GUI run; packaged-app
   behavior verified by Windows CI smoke.
3. Playwright Chromium cannot be downloaded locally → visual tests run authoritatively on
   Windows CI (confirmed blocked locally; do not retry the download).
4. Installed-app launch/restart/uninstall smoke: intentionally excluded from automated gating
   (same policy as v1.3.0) → **MANUAL USER VERIFICATION REQUIRED** in the final report.

## Files modified (this session, final set)

- Commits on `arena/01a0c8bf-dentiva-pro`: `deb2f3b` (governance docs) → … → `d45d6fa`
  (storage + service layer) → `b7e3e65` (IPC/preload/api/LocalRepo) → `1d714fb` (renderer + DS2.0
  + tests) → `d761c6b` (scale benchmark) → `7da5f6e` (visual spec rewrite) → `0ece474`
  (release docs + CI trigger fix). Full history via `git log --oneline`.
- Key files: `src/ops.js`, `src/queries.js`, `src/api.js`, `src/repo-local.js`, `src/main.js`,
  `src/styles.css`, `electron/main.mjs`, `electron/lib/*`, `electron/preload.cjs`,
  `tests/*` (55 tests), `tests/visual/layout.spec.mjs`, `scripts/dataset-benchmark.mjs`,
  `scripts/windows-smoke.ps1`, `.github/workflows/windows-release.yml` (push trigger now
  `arena/01a0c8bf-dentiva-pro`), root `CHANGELOG.md` (1.4.0 entry), `README.md`, `docs/*`.

## Tests completed / remaining

- `npm test`: **55/55 pass** (smoke, security, storage, workflows, domain, release-gate).
- `/tmp/opstest.mjs` (~60 checks) green vs SqlRepo AND vs LocalRepo (browser parity).
- `/tmp/bktest.mjs` backup/restore/diagnostics round-trip green (create, manifest, validate,
  tamper-reject, restore exact cents, safety backup, prune, containment).
- `/tmp/migtest.mjs` migration green (quarantine w/ reasons, dangling FK→NULL, exact
  `balance_cents`, externalized attachments, sanitized users, 0 FK violations, WAL,
  `.v4-preserved` kept, idempotent re-run, corrupt-primary `.bak` recovery).
- `npx vite build` green (338.75 kB / 38.10 kB, gzip 91.24 kB / 7.47 kB).
- Scale benchmark: 1k / 10k / 25k / 100k all interactive-range (see headline numbers).
- `npm audit`: 0 vulnerabilities.
- **First-run regression suite** (`tests/first-run.test.mjs`, 5 tests): setup→admin PIN→sign-in
  on SqlRepo (with `hashPin`/`verifyPin` round-trip) and on LocalApi (browser runtime, real
  WebCrypto KDF), last-Administrator protections, zero-admin store guard, self-deactivation
  guard, coverage-positive controls.
- jsdom harness over the production bundle: full first-run flow + 16 surfaces pass.
- Visual (6 viewports × 16 surfaces + accessible names): pending validation CI run #2
  (run `35734256848` = first attempt, failed the spec before the fixes; its failure was the
  signal that caught the three defects above).

## Build status

- `npx vite build` green locally. Windows packaging (portable + NSIS) produced only by CI.

## Release status

- **NOT YET PUBLISHED.** Validation run `35734256848` in progress (no publish token).
- Target artifacts (unchanged contract, new version):
  `Dentiva-Pro-1.4.0-Windows-x64.exe`, `Dentiva-Pro-1.4.0-Windows-x64-Setup.exe`,
  `Dentiva-Pro-1.4.0-Windows-x64.zip`, `Dentiva-Pro-1.4.0-checksums.txt`.
- Tag `v1.4.0` is created by the workflow's `gh release create` step (v1.3.0 precedent) —
  do NOT push the tag manually before the publish run.

## Exact next action

1. Push fix commits (no token) → watch validation run #2 (`gh run watch <id>`) to completion.
   If it fails again: CI logs are unreadable locally (blob EOF) — reproduce with the jsdom
   harness (see `docs/FINAL_AUDIT_REPORT_1.4.0.md` §3 for the method), fix, commit, re-push.
2. On green: commit a checkpoint update with message
   `v1.4.0: trigger controlled Windows release [publish-release]` and push.
3. Watch the publish run; on success fetch `gh release view v1.4.0 --json assets`
   (sizes + digests) and `gh run list --limit 1`.
4. Fill `docs/FINAL_AUDIT_REPORT_1.4.0.md` §5/§6 with run ID, artifact table, digests;
   update this file (phases all DONE, release published); commit docs (no token).
5. Only then declare: **Dentiva Pro v1.4.0 FINAL BUILD COMPLETE AND RELEASED.**

## Resume instructions

On "Continue": re-read this file; `git log --oneline -8` + `git status`; `gh run list --branch
arena/01a0c8bf-dentiva-pro --limit 2` to see whether run `35734256848` (validation) or a later
publish run is the newest; run `npm test` if source changed; resume from "Exact next action".
