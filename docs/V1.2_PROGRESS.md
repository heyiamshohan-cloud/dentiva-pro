# Dentiva Pro v1.2.0 persistent progress record

**Last updated:** 2026-09-22 (Asia/Dhaka)
**Branch:** `arena/01a0c66a-dentiva-pro`
**Rule:** resume from the first incomplete phase; never overwrite v1.0.0 or v1.1.0.

## Phase status

| Phase | Status | Evidence / next action |
|---|---|---|
| 1. Baseline and version identity | COMPLETE | Package, lockfile, renderer identity bumped to 1.2.0. Prior releases remain separate. |
| 2. SQLite persistence and migration | IMPLEMENTED / RUNTIME OPEN | `electron/storage.cjs`, Node tests and standalone smoke pass. Validate through Electron and Windows, including restart/crash/rollback. |
| 3. Core relational schema | COMPLETE FOR CURRENT MODEL | Schema v3 includes users and treatmentPlans; domain relationship/restore tests pass. Treatment-plan UI is still open. |
| 4. Users, authentication and RBAC | IMPLEMENTED / RUNTIME OPEN | First-run Administrator PIN, PBKDF2 hashes, sessions, lockout, staff links, roles and operation checks added. Run packaged multi-role matrix. |
| 5. Bengali localization | PARTIAL | Existing resource map/DOM layer expanded over prior release. Full professional page/dialog/print/PDF review remains open. |
| 6. Clinical/financial/operations feature gaps | OPEN | Treatment plans, financial statements, CSV mapping, queue intelligence and print profile refinements remain. |
| 7. Dataset/performance matrix | NOT STARTED | Generate 1k/5k/10k/25k realistic synthetic records outside production; measure startup/search/lists/profiles/timelines/reports/backup/restore. |
| 8. Windows automation | IMPLEMENTED / RUNTIME OPEN | `scripts/windows-smoke.ps1` now invokes the real Electron renderer smoke create/verify phases, portable restart, NSIS install/launch/uninstall and retains logs; a Windows runner must execute it. |
| 9. Visual regression | IMPLEMENTED / RUNTIME OPEN | Playwright config/test covers all six required resolutions and key surfaces; browser download failed at the sandbox TLS boundary, so run it on Windows/CI and retain artifacts. |
| 10. Full security/crash/import/restore QA | PARTIAL | Static and Node checks pass; packaged Electron fault injection and error UX remain. |
| 11. Documentation/release gate | IN PROGRESS | README, changelog, checklist, audit report and this record updated. Do not generate/publish v1.2 artifacts until blockers close. |

## Verified commands in this cycle

- `node --check src/main.js`
- `node --check src/core.js`
- `npm test` — 34 tests passed
- `npm run build` — Vite production build passed
- SQLite standalone storage tests — pass
- Vite preview returned HTTP 200 on the live preview port

## Known environment constraints

- The local Electron package has no executable; rebuilding hit the sandbox certificate boundary. Do not claim Electron GUI success from Linux source checks.
- Browser GUI automation is not installed in the sandbox. Windows/CI must provide the legitimate runtime and visual evidence.
- Direct large GitHub release asset downloads previously failed at the CDN boundary. Use the Windows runner's generated artifacts and checksums, then inspect GitHub metadata.

## Next actions, in order

1. Add and run operation-level tests for user sessions, role matrix, migration and renderer state transitions.
2. Finish treatment plans, patient statements, CSV mapping/import and actionable queue/notification behavior.
3. Expand Bengali translations and print/PDF copy profiles; review all dynamic errors and empty/loading states.
4. Add Playwright visual/DOM coverage and a Windows PowerShell smoke script; wire both to GitHub Actions.
5. Add dataset generation/benchmark logs for 1k, 5k, 10k and 25k patients where practical.
6. Run `npm run check`, Windows packaging and release self-validation; update the audit/checklist from OPEN to evidence-backed statuses.
7. Only then tag/publish v1.2.0 with the four exact artifacts and preserve v1.0.0/v1.1.0.
